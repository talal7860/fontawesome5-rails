RSpec.describe Fontawesome5::Rails do
  it 'has a version number' do
    expect(Fontawesome5::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
